export interface Evidence {
  title: string;
  description: string;
  imageUrl: string;
  additionalImages: string[];
}

export interface Profile {
  name: string;
  description: string;
  imageUrl: string;
  age: string;
}
